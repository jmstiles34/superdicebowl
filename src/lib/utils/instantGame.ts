// Barrel re-export: football instant game simulation
// During migration, all existing imports continue to work unchanged.
// Once imports are updated to use $lib/football/utils/instantGame directly,
// this file can be removed.

export { simulateInstantGame } from '$lib/football/utils/instantGame';
